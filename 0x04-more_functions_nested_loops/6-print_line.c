#include "main.h"

/**
 * print_line - Print a straight line of characters.
 * @n: The number of times the character '_' should be printed.
 */
void print_line(int n)
{
int i;

if (n > 0)
{
for (i = 0; i < n; i++)
{
putchar('');
}
}
_putchar('\n');
}
